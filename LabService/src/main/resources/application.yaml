# ============================================================
# LabService — Spring Boot Configuration
# ============================================================

spring:
  application:
    name: lab-service

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

  # ==================== DATABASE ====================
  datasource:
    url: ${LAB_DATASOURCE_URL:jdbc:oracle:thin:@localhost:1522/XEPDB1}
    driver-class-name: oracle.jdbc.OracleDriver
    username: ${LAB_DATASOURCE_USERNAME:lab}
    password: ${LAB_DATASOURCE_PASSWORD:lab123}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5

  # ==================== JPA ====================
  jpa:
    database: ORACLE
    hibernate:
      ddl-auto: ${LAB_JPA_DDL_AUTO:update}
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
        format_sql: true

  # ==================== JACKSON ====================
  jackson:
    serialization:
      indent-output: true
    deserialization:
      fail-on-unknown-properties: false
    default-property-inclusion: non_null

  # ==================== CACHE ====================
  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=1000,expireAfterWrite=300s

# ==================== SERVER ====================
server:
  port: ${SERVER_PORT:8087}

# ==================== SERVICES ====================
services:
  auth:
    base-url: ${AUTH_SERVICE_BASE_URL:http://localhost:8281}
  core:
    base-url: ${CORE_SERVICE_BASE_URL:http://localhost:8082}

# ==================== JWT ====================
app:
  jwt:
    secret: ${JWT_SECRET:dev-only-insecure-secret-change-me-in-production-0123456789abcdef}

# ==================== SPRING DOCS (OpenAPI/Swagger) ====================
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha
    doc-expansion: none
    display-request-duration: true
    filter: true
  packages-to-scan: hospital.labservice.controller
  paths-to-match: /api/v1/**

# ==================== LOGGING ====================
logging:
  level:
    root: INFO
    hospital.labservice: DEBUG
    org.springframework.security: INFO
